const config = {
  host: "localhost",
  database: "defaultdev12",
  username: "admin",
  password: "asdf",
  port: "8069"
};

module.exports = config;

// config.host;
// this.port = config.port || 80;
// this.database = config.database;
// this.username = config.username;
// this.password = config.password;
